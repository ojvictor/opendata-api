services:
  db: 
    image: postgres
    env_file:
      - .db-development.env
    volumes:
      - ./storage/development/db:/var/lib/postgresql/data

  web:
    build: .
    env_file:
      - .db-development.env
    command: bash -c "rm -rf tmp/server.pid && bundle exec rails s -p 3000 -b 0.0.0.0"
    volumes:
      - .:/ticweb-api
    ports: 
    - "3000:3000"
    depends_on:
      - db
